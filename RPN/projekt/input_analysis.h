#pragma once

//function reads a number from a given spot in a string
int ReadInt(int * spot, char * buffer);

////function reads an operation from a given spot in a string
operation ReadOperation(int * spot, char * buffer);
